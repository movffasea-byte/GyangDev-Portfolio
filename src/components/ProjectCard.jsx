export default function ProjectCard({ project }) {
  return <article className={`project-card${project.featured ? " project-featured" : ""}`}>{project.placeholder ? <div className="project-preview project-preview-studio" aria-label="Jai'fore Admin project image placeholder"><span>{project.number} / ECOSYSTEM</span><strong>Jai'fore<br />Admin</strong><small>Project image to be added</small></div> : <div className="project-preview project-preview-media"><img src={project.image} alt={project.imageAlt} /></div>}<div className="project-number">{project.number}</div><div className="project-content"><p className="project-type">{project.type}</p><h3>{project.title}</h3><p>{project.description}</p>{project.points && <ul className="project-points">{project.points.map((point) => <li key={point}>{point}</li>)}</ul>}{project.technologies && <div className="tags" aria-label="Technology used">{project.technologies.map((technology) => <span key={technology}>{technology}</span>)}</div>}{project.links.map(([label, href], index) => <a className={`text-link${index ? " project-repository" : ""}`} href={href} target="_blank" rel="noopener" key={href}>{label} <span aria-hidden="true">↗</span></a>)}</div></article>;
}
